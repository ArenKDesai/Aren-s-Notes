#CS513 #LinearAlgebra #Deflation #UWMadison #Factorizing

If $A_{mxm}$, there is an $A=QUQ'$, where if A is symmetric, then U is diagonal. 

## Deflation
- Input: $A_{mxm}$, 
- Output of one step: one eigenpair, one deflated A with the remaining eigenpairs

### Actual Step
- Input: $v$, which is normalized

$H_1v=e_1$
$B_1=H_1A_0H_1$
$B_1e_1=\lambda e_1$
