We've set up processes to have virtual memory, but how do we decide how much memory a process needs? 
We define the working set for a process at time $t$ as $W(D,t)$, where $D$ is a window of time and $W$ 